<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="8dp"
    android:background="#1E1E1E"> <!-- Grey background -->

    <!-- City Name and Temperature -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_weight="1"
        android:paddingEnd="8dp">

        <TextView
            android:id="@+id/cityNameTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="City Name"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="#FFFFFF" /> <!-- White text -->

        <TextView
            android:id="@+id/cityTemperatureTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="--°C"
            android:textSize="16sp"
            android:textColor="#A0A0A0" /> <!-- Light grey text -->
    </LinearLayout>

    <!-- Remove Button -->
    <Button
        android:id="@+id/removeFavoriteButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:text="Remove"
        android:padding="8dp"
        android:textColor="#FFFFFF"
        android:backgroundTint="#008080" /> <!-- Teal button -->
</LinearLayout>
